import { Button, Cascader, Checkbox, Col, Input, Radio, Row } from 'antd';
import Form, { useForm } from 'antd/lib/form/Form';
import React from 'react';

const PersonPage = () => {
  return <></>;
};

export default PersonPage;
